# geospatial

Owns search normalization and coordinate resolution.

All city search and latitude/longitude search must resolve into coordinates before analysis begins.

Runtime implementation currently lives in `apps/api/app/services/geospatial`.

