# alert-engine

Owns alert generation from disaster signals.

The alert engine converts explainable hazard findings into operator-facing alerts with severity, title, and message fields.

Runtime implementation currently lives in `apps/api/app/services/alert_engine`.

