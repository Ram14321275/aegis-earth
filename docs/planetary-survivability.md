# Planetary Survivability

The `ResilienceSimulationEngine` provides `/simulations/run`.
Operators can simulate a `provider_collapse` or `region_partition`.
The engine runs purely in isolation, projecting theoretical recovery timelines and confidence degradation without actually perturbing the live infrastructure.
